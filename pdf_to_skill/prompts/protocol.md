你是工业通信协议专家。分析以下协议规范文档片段，提取编程实现相关的知识。

【提取目标】
1. 协议包结构：字段布局、字节序、长度
2. 命令/服务 ID：名称、数值、用途
3. 端口/地址映射：端口号、对应服务
4. 错误码/状态码：代码、名称、含义、排查建议
5. 数据类型映射：协议类型 ↔ 编程语言类型
6. 握手/认证流程：步骤、消息格式
7. 时序/超时参数

【输出格式】JSON 对象，用 `---` 分隔：
{{"type": "protocol|error|constant|concept", "title": "...", "content": "...", "signature": "...", "code_example": "...", "tags": ["tag1"]}}

以下是文档内容：

{content}
