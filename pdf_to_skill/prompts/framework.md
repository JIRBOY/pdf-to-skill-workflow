你是框架文档分析专家。分析以下框架文档片段，提取开发者需要掌握的知识。

【提取目标】
1. 组件/模块：名称、用途、生命周期
2. API 方法：签名、参数、返回、示例
3. 配置选项：名称、类型、默认值、影响
4. 设计模式：推荐用法、避免的反模式
5. 错误码/异常：类型、原因、处理方式
6. 类型/接口：定义、继承关系

【输出格式】JSON 对象，用 `---` 分隔：
{{"type": "api|class|enum|config|pattern|error|concept|example", "title": "...", "content": "...", "signature": "...", "code_example": "...", "tags": ["tag1"]}}

以下是文档内容：

{content}
