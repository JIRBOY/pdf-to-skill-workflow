你是 API/SDK 文档分析专家。分析以下技术文档片段，提取所有编程相关知识。

【提取目标】
1. API 方法/函数：完整签名、参数说明、返回类型、代码示例
2. 类/接口/结构体：成员、继承关系、用途
3. 枚举值：名称、数值、含义
4. 常量：名称、值、用途
5. 配置项：配置名、类型、默认值
6. 错误码：代码、名称、原因、排查建议
7. 使用模式：最佳实践、常见陷阱

【输出格式】每个知识点用 JSON 对象表示：
{{"type": "api|class|enum|constant|config|error|example|pattern|concept", "title": "...", "content": "...", "signature": "...", "code_example": "...", "tags": ["tag1", "tag2"]}}

【规则】
- 代码示例必须完整可运行，不是伪代码
- 签名包含完整参数和返回类型
- tags 至少包含所属模块名
- 只输出编程相关内容，忽略纯理论说明
- 一个知识点一个 JSON 对象，用 `---` 分隔

以下是文档内容：

{content}
