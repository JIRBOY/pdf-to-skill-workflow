你是内部文档分析专家。分析以下公司内部文档片段，提取编程实现相关的知识。

【提取目标】
1. API 接口：路径、方法、参数、响应格式
2. 枚举/常量：业务含义、取值范围
3. 业务规则：条件、限制、边界
4. 数据结构：字段、类型、校验规则
5. 错误码：代码、含义、处理逻辑

【输出格式】JSON 对象，用 `---` 分隔：
{{"type": "api|enum|constant|concept|error", "title": "...", "content": "...", "signature": "...", "code_example": "...", "tags": ["tag1"]}}

以下是文档内容：

{content}
